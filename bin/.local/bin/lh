#!/bin/sh

url="$(echo "$1" | sed 's/?.*$//')"

case $url in
*.png|*.jpg) curl $url | setsid -f $IMAGE_VIEWER -;;
*.mp3|*.ogg|*youtube.com/watch*) setsid -f mpv --force-window $url > /dev/null 2>&1 & ;;
*) setsid -f $BROWSER $url > /dev/null 2>&1 & ;;
esac
